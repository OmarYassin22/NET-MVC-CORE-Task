@model ARIBApp.Core.Contracts.TaskDTO
@{
    ViewData["Title"] = $"Create Task for {ViewBag.Employee.FullName}";
    Layout = "_EmployeeLayout";
}

<div class="card">
    <div class="card-header">
        <h5 class="mb-0"><i class="bi bi-plus-circle me-2"></i>Create New Task</h5>
    </div>
    <div class="card-body">
        @await Html.PartialAsync("_CreateTaskForm", Model)
    </div>
</div>